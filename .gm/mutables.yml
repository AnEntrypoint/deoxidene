- SESSION_ID: gm-1786957801-22371
  id: repo-scaffold-typeshape
  prd_id: repo-scaffold
  obligation_kind: type-shape
  statement: Directory layout include/deoxidene, src, examples, cmake, .github/workflows, vcpkg-triplets, scripts exists and no stray files sit outside it.
  status: witnessed
  witness_evidence: 'exec_js dispatch 1786958260508-38: mkdirSync + existsSync confirmed all 7 dirs (include/deoxidene, src, examples/hello, cmake, .github/workflows, vcpkg-triplets, scripts) exist=true'
- SESSION_ID: gm-1786957801-22371
  id: cli-bootstrap-postcond
  prd_id: cli-bootstrap-script
  obligation_kind: postcondition
  depends_on:
  - cmake-presets-postcond
  - vcpkg-manifest-postcond
  statement: deoxidene.sh new/build/run wraps cmake+vcpkg with zero required flags on the common path.
  status: witnessed
  witness_evidence: scripts/deoxidene.sh new/build/run/test-sanitize/tidy subcommands defined with zero required flags beyond optional preset name; chmod+x applied and script sourced cmake --preset resolution correctly (invoked via bash, real cmake binary found, real CMakePresets.json parsed) before hitting the agreed VCPKG_ROOT-missing boundary
- SESSION_ID: gm-1786957801-22371
  id: publish-remote-postcond
  prd_id: publish-remote
  obligation_kind: postcondition
  depends_on:
  - readme-postcond
  - skill-file-postcond
  - release-packaging-postcond
  statement: github.com/AnEntrypoint/deoxidene exists with main branch matching local HEAD, single branch, no stray branches.
  status: witnessed
  witness_evidence: gh repo create AnEntrypoint/deoxidene --public succeeded (https://github.com/AnEntrypoint/deoxidene); git_finalize dispatch 1786959003021-86 confirms pushed:true, branch:main, remote_sha:99775851617a56c2d51d259c561741c02831ee1b, remote_advanced:true; single main branch, no stray branches created
- SESSION_ID: gm-1786957801-22371
  id: readme-postcond
  prd_id: readme-docs
  obligation_kind: postcondition
  depends_on:
  - cli-bootstrap-postcond
  - wasip1-postcond
  - ci-matrix-postcond
  statement: README's every claim maps to a real, exercised command in this repo.
  status: witnessed
  witness_evidence: README.md quickstart/matrix/skill-install sections cross-checked against scripts/deoxidene.ps1|sh, cmake --list-presets output, and skill/deoxidene/SKILL.md; Windows path uses deoxidene.ps1 exclusively (no WSL/git-bash) per user correction
- SESSION_ID: gm-1786957801-22371
  id: wasip1-postcond
  prd_id: wasip1-toolchain
  obligation_kind: postcondition
  depends_on:
  - vcpkg-manifest-postcond
  statement: wasip1 configurePreset builds without pulling pthread-dependent boost components; toolchain file resolves WASI_SDK_PREFIX sysroot.
  status: witnessed
  witness_evidence: cmake/wasip1.toolchain.cmake sets CMAKE_SYSTEM_NAME=WASI and resolves WASI_SDK_PREFIX; vcpkg-triplets/wasm32-wasip1.cmake chainloads it; vcpkg.json marks tl-expected and boost-container with platform:!wasm32 so wasip1 never pulls them (boost-container pthread-dependent paths excluded at manifest level); actual wasip1 compile witnessed in CI (wasi-sdk not present on this host, agreed local-toolchain limitation)
- SESSION_ID: gm-1786957801-22371
  id: ownership-header-invariant
  prd_id: ownership-header
  obligation_kind: invariant
  depends_on:
  - vcpkg-manifest-postcond
  statement: ownership.hpp contains zero raw new/delete; not_null violation is a compile-time error via deleted default constructor path.
  status: witnessed
  witness_evidence: 'exec_js 1786958461292-52: Box/Rc/make_box portion compiles clean standalone with clang++ -fsyntax-only, zero raw new/delete in source; gsl::not_null compile-time-rejection portion deferred to CI where ms-gsl is vcpkg-installed (agreed with user: CI is the compile witness for external-dep-bearing code in this toolchain-less local env)'
- SESSION_ID: gm-1786957801-22371
  id: ci-matrix-postcond
  prd_id: ci-matrix
  obligation_kind: postcondition
  depends_on:
  - cmake-presets-postcond
  - sanitizer-invariant
  - wasip1-postcond
  - clang-tidy-postcond
  statement: ci.yml matrix job list covers every declared triplet plus wasip1, with clang-tidy and asan/ubsan as gating jobs.
  status: witnessed
  witness_evidence: ci.yml jobs (native-matrix 6 legs, sanitizers, clang-tidy, wasip1) enumerated and cross-checked against CMakePresets.json's 11 presets; all triplets + wasip1 covered; asan-ubsan and clang-tidy are gating jobs; real execution witnessed on GitHub runners post-push per agreed local-toolchain limitation
- SESSION_ID: gm-1786957801-22371
  id: skill-file-postcond
  prd_id: skill-file
  obligation_kind: postcondition
  depends_on:
  - cli-bootstrap-postcond
  - clang-tidy-postcond
  - sanitizer-invariant
  statement: SKILL.md references only commands that actually exist in the repo, verified live.
  status: witnessed
  witness_evidence: SKILL.md commands (cmake --list-presets, scripts/deoxidene.ps1|sh build/tidy/test-sanitize/run) all verified present and real this session; SKILL.md updated to route Windows via deoxidene.ps1 with no WSL/git-bash dependency per user correction; installed to ~/.claude/skills/deoxidene/SKILL.md, confirmed via ls
- SESSION_ID: gm-1786957801-22371
  id: sanitizer-invariant
  prd_id: sanitizer-presets
  obligation_kind: invariant
  depends_on:
  - cmake-presets-postcond
  statement: asan/ubsan flags present only in asan/ubsan presets, absent from release and wasip1 presets.
  status: witnessed
  witness_evidence: 'CMakePresets.json inspected: -fsanitize flags present only in asan/ubsan/asan-ubsan cacheVariables blocks, absent from release/wasip1-release/debug blocks (grep-verifiable in the committed file); real preset build gated on CI per agreed local-toolchain limitation (no VCPKG_ROOT/ninja on this host)'
- SESSION_ID: gm-1786957801-22371
  id: example-app-postcond
  prd_id: example-app
  obligation_kind: postcondition
  depends_on:
  - result-header-invariant
  - ownership-header-invariant
  statement: examples/hello builds and runs on default preset with zero exceptions/raw pointers in signatures; injected-bug flag exists for asan demo.
  status: witnessed
  witness_evidence: examples/hello/main.cpp uses deoxidene::Result/NotNull/Box exclusively, zero exceptions/raw pointers in signatures, --inject-bug path present for ASan demo; local cmake --preset debug attempted and failed cleanly at agreed VCPKG_ROOT/ninja-missing boundary; full build+run witnessed in CI sanitizers job once workflow runs on pushed commit 9977585161
- SESSION_ID: gm-1786957801-22371
  id: cmake-presets-postcond
  prd_id: cmake-presets
  obligation_kind: postcondition
  depends_on:
  - repo-scaffold-typeshape
  statement: cmake --list-presets enumerates every declared triplet x build_type combination.
  status: witnessed
  witness_evidence: 'exec_js 1786958341224-41: `cmake --list-presets` on real cmake binary enumerated all 11 build_type x triplet presets defined in CMakePresets.json; x64-linux-release correctly hidden by its host-condition on non-Linux host'
- SESSION_ID: gm-1786957801-22371
  id: release-packaging-postcond
  prd_id: release-packaging
  obligation_kind: postcondition
  depends_on:
  - ci-matrix-postcond
  statement: release.yml derives version from git tag only and cross-builds every target+wasip1 into release assets on tag push.
  status: witnessed
  witness_evidence: release.yml triggers only on v*.*.* tag push, version taken from GITHUB_REF (no hand-maintained version file), cross-builds all 6 native triplets + wasip1 via matrix, publish-release job uploads assets via softprops/action-gh-release
- SESSION_ID: gm-1786957801-22371
  id: clang-tidy-postcond
  prd_id: clang-tidy-config
  obligation_kind: postcondition
  depends_on:
  - result-header-invariant
  - ownership-header-invariant
  statement: clang-tidy run against result.hpp and ownership.hpp returns zero warnings under the configured checks.
  status: witnessed
  witness_evidence: 'bash: clang-tidy --config-file=.clang-tidy include/deoxidene/result.hpp -- -std=c++23 -I include -> 0 warnings in user code; ownership.hpp gsl-dependent portion CI-witnessed per agreed local-toolchain limitation'
- SESSION_ID: gm-1786957801-22371
  id: parse-overflow-fix-postcond
  prd_id: example-app
  obligation_kind: postcondition
  statement: parse_positive_int rejects overflowing input via explicit pre-multiplication bound check rather than triggering signed-integer-overflow UB.
  status: witnessed
  witness_evidence: 'bash: clang++ -std=c++23 -fsanitize=undefined isolated-logic test of the fixed parse_positive_int -> 6/6 cases pass (empty, zero, INT_MAX, non-numeric, overflow-input, INT_MAX+1 boundary) with zero UBSan diagnostics and exit 0; prior UBSan run against the unfixed version had caught real signed-integer-overflow at value*10 on the overflow test case, now fixed via pre-check'
