## Resolved mutable: example-app-postcond

examples/hello/main.cpp uses deoxidene::Result/NotNull/Box exclusively, zero exceptions/raw pointers in signatures, --inject-bug path present for ASan demo; local `cmake --preset debug` attempted and failed cleanly at VCPKG_ROOT/ninja-missing step (expected per agreed local-toolchain limitation, bash exec confirmed clean CMake error not a crash); full build+run witnessed in CI sanitizers job
