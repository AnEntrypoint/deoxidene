## Resolved mutable: skill-file-postcond

SKILL.md commands (cmake --list-presets, scripts/deoxidene.ps1|sh build/tidy/test-sanitize/run) all verified present and real this session; SKILL.md updated to route Windows via deoxidene.ps1 with no WSL/git-bash dependency per user correction; installed to ~/.claude/skills/deoxidene/SKILL.md, confirmed via ls
